export interface MaskTransformConfig {
    typeName: string;
    fieldName: string;
    mask: string;
}

export interface MaskTransformAlias {
    type: string;
    name: string;
    alias: string;
}
